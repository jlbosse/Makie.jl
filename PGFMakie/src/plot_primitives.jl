#===============================================================================
# This is where the whole drawing pipeline lives
# The logic is mostly stolen from CairoMakie/src/plot-primitives.jl
===============================================================================#

function pgf_draw(screen::Screen, scene::Scene)
end
